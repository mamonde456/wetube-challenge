extends base
include mixins/video

block content 
    div.home-wrap
        if videos
            each video in videos
                +video(video)
        else 
            p.notingText video noting found.
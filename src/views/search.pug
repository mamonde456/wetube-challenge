extends base
include mixins/video

block content 
    div
        form(method="get") 
            input(placeholder="Write the word to search" type="text" required name="keyword")
            button send
    hr
    div 
        each video in videos
            +video(video)